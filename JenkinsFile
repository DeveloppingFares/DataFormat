pipeline {
    agent {label 'master'}
    options {
        buildDiscarder logRotator(
                    daysToKeepStr: '16',
                    numToKeepStr: '10'
            )
        disableConcurrentBuilds()
    }
    triggers { cron( 'H/2 * * * *' ) }
    stages {
        stage('Hello') {
            steps {
                echo "Hello"
            }
        }
    }
}
